server:
  port: 7000
spring:
  application:
    name: paocai-coupon
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://192.168.136.12:3306/paocai-sms
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.136.12:8848
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto  # 主键位置
# 暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: '*'